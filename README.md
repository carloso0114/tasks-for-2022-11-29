** Requirements: 
- Node Js 

** Instructions:
* Run the js with node using the cmd: Example 'node .\generateHashtag.js'

** Notes:
* If you wish to change the arguments for the function called, just edit the js file , there is one console log calling the functions there.